# Slash command module tests

These tests cover catalog merging, private image assets, owner dispatch, replacement events,
validation failures, and turn-time refresh behavior.

```text
fake contributors -> SlashCommandsModule -> durable EventsModule replay
```
