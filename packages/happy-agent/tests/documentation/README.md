# Documentation extraction tests

These tests exercise the release-neutral extraction behavior with temporary packaged docs and a
temporary Happy home. Standalone embedding is assembled at the binary build boundary.
