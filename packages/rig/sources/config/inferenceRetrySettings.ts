export { DEFAULT_INFERENCE_MAX_RETRIES, MAX_INFERENCE_MAX_RETRIES } from "@slopus/happy-providers";

/** Must match @slopus/happy-providers's DEFAULT_INFERENCE_FATAL_RETRIES. */
export const DEFAULT_INFERENCE_FATAL_RETRIES = 0;
