name: Publish package

on:
    push:
        branches:
            - main
        tags:
            - "rig-v*"
            - "happy-agent-base-v*"
            - "happy-plugins-v*"
            - "happy-providers-v*"
    workflow_dispatch:

env:
    RELEASE_PACKAGE: ${{ startsWith(github.ref_name, 'happy-agent-base-v') && 'happy-agent-base' || startsWith(github.ref_name, 'happy-plugins-v') && 'happy-plugins' || startsWith(github.ref_name, 'happy-providers-v') && 'happy-providers' || 'rig' }}

permissions:
    contents: read
    id-token: write

concurrency:
    group: publish-${{ github.ref }}
    # Only the newest canary build of main is worth finishing.
    cancel-in-progress: ${{ github.ref == 'refs/heads/main' }}

jobs:
    publish:
        name: Publish selected package
        if: startsWith(github.ref, 'refs/tags/')
        runs-on: ubuntu-latest
        environment: npm
        timeout-minutes: 30
        env:
            RELEASE_DIST_TAG: ${{ contains(github.ref_name, '-beta.') && 'beta' || '' }}

        steps:
            - name: Check out the release tag
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0

            - name: Set up pnpm
              uses: pnpm/action-setup@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: "24"
                  package-manager-cache: false

            - name: Install Linux sandbox dependencies
              if: ${{ !contains(github.ref_name, '-beta.') }}
              run: |
                  sudo apt-get update
                  sudo apt-get install --yes bubblewrap ripgrep socat
                  if [[ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]]; then
                      sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
                  fi

            - name: Verify the tagged commit is on main
              run: |
                  if ! git merge-base --is-ancestor HEAD origin/main; then
                      echo "Release tags must point to a commit on the main branch." >&2
                      exit 1
                  fi

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Verify the tag matches the package version
              env:
                  RELEASE_TAG: ${{ github.ref_name }}
              run: pnpm exec tsx scripts/validateReleaseTag.ts

            - name: Run type checks
              run: pnpm exec tsx scripts/runReleasePackageValidation.ts check

            - name: Run tests
              if: ${{ !contains(github.ref_name, '-beta.') }}
              env:
                  # Keep Sandbox Runtime's home-directory mask away from the checkout.
                  HOME: ${{ runner.temp }}/rig-test-home
              run: |
                  mkdir -p "$HOME"
                  pnpm exec tsx scripts/runReleasePackageValidation.ts test

            - name: Build the package
              run: |
                  case "$RELEASE_PACKAGE" in
                      happy-agent-base) pnpm --filter @slopus/happy-agent-base build ;;
                      happy-plugins) pnpm --filter happy-plugins build ;;
                      happy-providers) pnpm --filter @slopus/happy-providers build ;;
                      *) pnpm run build ;;
                  esac

            - name: Publish the package
              run: pnpm exec tsx scripts/publishPackage.ts

    canary:
        name: Publish the canary builds
        if: github.ref == 'refs/heads/main' && github.repository == 'slopus/rig'
        runs-on: ubuntu-latest
        environment: npm
        timeout-minutes: 20

        steps:
            - name: Check out the commit
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0

            - name: Set up pnpm
              uses: pnpm/action-setup@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: "24"
                  package-manager-cache: false

            - name: Install Linux sandbox dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install --yes bubblewrap ripgrep socat
                  if [[ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]]; then
                      sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
                  fi

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            # A resolvable published side-package canary is its watermark, keeping later package
            # changes pending across failed workflows.
            - name: Check for unpublished side-package changes
              id: changed
              env:
                  PUSHED_FROM: ${{ github.event.before }}
              run: pnpm exec tsx scripts/detectCanaryPackageChanges.ts

            - name: Set the canary versions
              env:
                  CANARY_BUILD_NUMBER: ${{ github.run_number }}
                  CANARY_COMMIT: ${{ github.sha }}
                  PLUGINS_CHANGED: ${{ steps.changed.outputs.plugins }}
                  PROVIDERS_CHANGED: ${{ steps.changed.outputs.providers }}
              run: |
                  # happy-plugins is versioned before the build so the SDK Rig bundles carries the
                  # same canary version that is published.
                  if [[ "$PLUGINS_CHANGED" == "true" ]]; then
                      RELEASE_PACKAGE=happy-plugins pnpm exec tsx scripts/setCanaryVersion.ts
                  fi
                  if [[ "$PROVIDERS_CHANGED" == "true" ]]; then
                      RELEASE_PACKAGE=happy-providers pnpm exec tsx scripts/setCanaryVersion.ts
                  fi
                  RELEASE_PACKAGE=rig pnpm exec tsx scripts/setCanaryVersion.ts

            - name: Build the packages
              env:
                  PROVIDERS_CHANGED: ${{ steps.changed.outputs.providers }}
              run: |
                  pnpm run build
                  if [[ "$PROVIDERS_CHANGED" == "true" ]]; then
                      pnpm --filter @slopus/happy-providers build
                  fi

            # Canary builds stay fast: unit tests only, no gym, Docker, or live-provider suites.
            - name: Run unit tests
              env:
                  # Keep Sandbox Runtime's home-directory mask away from the checkout.
                  HOME: ${{ runner.temp }}/rig-test-home
                  PLUGINS_CHANGED: ${{ steps.changed.outputs.plugins }}
                  PROVIDERS_CHANGED: ${{ steps.changed.outputs.providers }}
              run: |
                  mkdir -p "$HOME"
                  pnpm run test:scripts
                  pnpm --recursive --filter './packages/**' --filter '!@slopus/rig-dev' --filter '!@slopus/rig-gym-tests' --filter '!happy-plugins' --filter '!@slopus/happy-providers' --filter '!@slopus/rig-codemode-codex' run test
                  if [[ "$PLUGINS_CHANGED" == "true" ]]; then
                      pnpm --filter happy-plugins test
                  fi
                  if [[ "$PROVIDERS_CHANGED" == "true" ]]; then
                      pnpm --filter @slopus/happy-providers test
                  fi

            - name: Publish the canary builds
              env:
                  PLUGINS_CHANGED: ${{ steps.changed.outputs.plugins }}
                  PROVIDERS_CHANGED: ${{ steps.changed.outputs.providers }}
                  RELEASE_DIST_TAG: canary
              run: |
                  if [[ "$PLUGINS_CHANGED" == "true" ]]; then
                      RELEASE_PACKAGE=happy-plugins pnpm exec tsx scripts/publishPackage.ts
                  fi
                  if [[ "$PROVIDERS_CHANGED" == "true" ]]; then
                      RELEASE_PACKAGE=happy-providers pnpm exec tsx scripts/publishPackage.ts
                  fi
                  RELEASE_PACKAGE=rig pnpm exec tsx scripts/publishPackage.ts
